server:
  port: 8082
spring: 
  application:
    name: demo-swagger
logging:
  level:
    root: INFO
    org.springframework.security: INFO
    com.mybatis: DEBUG

management:
  security:
    enabled: false

security:
  basic:
    enabled: false

server:
  context-path: swagger